- defaults:
    link:
        create: true
        relink: true

- clean: ['~']

- link:
    ~/.bashrc:              bashrc
    ~/.gitconfig:           gitconfig
    ~/.vim:                 vim/
    ~/.vimrc:               vimrc

- shell:
    - [git submodule update --init --recursive, Installing submodules]

